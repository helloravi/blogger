<h1>List of All the tags</h1>
<ul>
<%@tags.each do |tag|%>
<%= link_to tag.name, tag_path(tag) %>
<%= link_to "Delete", tag_path(tag), method: :delete, data: {confirm: "Do you really want to delete the tag?"} %>

<br/>
<%end%>
</ul>
